"""Orchestrator-side helpers (ARG1-021+ ).

Modules under this package are consumed by the orchestrator agent and by
ARG1-022's parallel-dispatch loop. Each module is stdlib-only per ADR-001
and emits machine-parseable output for the orchestrator.
"""
