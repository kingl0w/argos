# {{PREFIX}}-001

**Status:** Queued
**Created:** {{DATE}}
**Priority:** P2

## Intent

_One or two sentences. What changes in the world after this ticket merges? Write it as an outcome, not a task list._

## Context

_What the reader needs to know to understand why this ticket exists. Link to PRD sections, prior tickets, ADRs, or external references. If an invariant from `.specs/ARCHITECTURE.md` is relevant, cite it by name._

## Non-goals

- _Adjacent work that someone might reasonably assume is included but isn't. Naming these prevents the coder from expanding scope._
- _Behaviors we explicitly will not support in this ticket._

## Acceptance criteria (draft)

_The planner will refine these into executable test cases. Start with observable behaviors; the planner adds specific inputs and expected outputs._

- [ ] _Behavior the verifier can check by running a command and reading output._
- [ ] _Edge case that must be handled (error path, empty input, boundary condition)._
- [ ] _Regression not introduced: name the existing behavior that must still work._

<!--
  The following sections are appended by Argos subagents during `/next`:

  ## Plan
      Written by the planner subagent. Step-by-step implementation approach,
      files to touch, tests to add. Cites relevant ADRs and architecture
      invariants. Read-only with respect to code.

  ## Implementation notes
      Written by the coder subagent as it executes the plan. Records
      deviations from the plan (with justification) and any follow-up
      tickets to file. Does not modify the Plan section above.

  ## Verification
      Written by the verifier subagent after running tests and checking
      acceptance criteria. Must quote real stdout from the test command;
      hallucinated "tests pass" output is a P0 bug. On pass, verifier
      also updates `.specs/STATE.md`.

  Do not write these sections by hand. If the loop fails before a section
  is filled, run `/steer` rather than filling it yourself.
-->
